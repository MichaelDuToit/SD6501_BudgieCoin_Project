<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sd6501_budgiecoin_project">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".RegistrationActivity"
            android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".TransactionActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TransactionUpdateActivity"
            android:parentActivityName=".ViewTransactionsActivity" />
        <activity
            android:name=".ViewAccountActivity"
            android:parentActivityName=".AccountBalancesActivity" />
        <activity
            android:name=".AddAccountActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".AppCompatNavigationDrawerActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewTransactionsActivity" />
        <activity android:name=".AccountBalancesActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>